fastapi
uvicorn[standard]
python-dotenv
psycopg[binary]
passlib[bcrypt]
python-jose[cryptography]
PyJWT
jinja2

# --- Dépendances pour corriger le bug Supabase/proxy ---
# On spécifie des versions précises pour garantir la compatibilité.
httpx>=0.25.0
supabase>=2.0.0
